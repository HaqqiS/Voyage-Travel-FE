const endpoint = {
    AUTH: "/auth",
    TOUR: "/tours",
    DESTINATION: "/destinations",
    PARTICIPANT: "/participants",
    BANNER: "/banners",
    ORDER: "/orders",
    
};

export default endpoint;
